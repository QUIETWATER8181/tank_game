# 钢铁征途：公开发布说明

游戏是纯静态网页，不需要服务器端程序、数据库或构建步骤。发布时只需要把 `发布包` 目录中的内容上传到静态网站托管平台，平台会生成一个 HTTPS 网址，其他人打开网址即可直接游玩。

## 最快方式：Netlify Drop

1. 打开 [Netlify Drop](https://app.netlify.com/drop)。
2. 将本目录中的 `发布包` 文件夹整体拖入页面中央的上传区域。
3. 等待上传完成，复制平台生成的 `https://...netlify.app` 地址并分享。

上传的是文件夹内容本身，网址打开后必须直接显示游戏首页；如果平台提示选择发布目录，请选择 `发布包`。

## 稳定方式：GitHub Pages

1. 在 GitHub 新建一个公开仓库，例如 `steel-journey`。
2. 将 `发布包` 内的 `index.html`、`css`、`js`、`assets`、`music` 上传到仓库根目录。
3. 进入仓库的 `Settings` -> `Pages`。
4. 在 `Build and deployment` 中选择 `Deploy from a branch`，分支选 `main`，目录选 `/ (root)`，点击保存。
5. 等待几分钟后，访问并分享 GitHub Pages 给出的 `https://用户名.github.io/仓库名/` 地址。

## 发布前检查

- 不要只上传 `index.html`，必须同时上传 `css`、`js`、`assets` 和 `music`。
- 建议使用 HTTPS 地址；浏览器对音频和全屏功能的限制会更少。
- 第一次进入时，玩家需要点击游戏内按钮后浏览器才会允许播放背景音乐，这是浏览器的自动播放策略。
- 当前发布包约 18 MB，包含完整运行资源，不包含测试、工具和 4399 平台专用代码。

## 本地预览

在项目目录执行：

```powershell
npx --yes http-server 发布包 -p 8080
```

然后打开 `http://localhost:8080`。不要直接双击 HTML 文件作为最终兼容性测试，因为部分浏览器会限制 `file://` 页面中的音频或资源加载。
